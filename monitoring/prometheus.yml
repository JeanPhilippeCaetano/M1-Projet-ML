global:
  scrape_interval: 15s  # Intervalle de collecte des métriques

scrape_configs:
  - job_name: "node_exporter"
    static_configs:
      - targets: ["node_exporter:9100"]  # Utiliser le nom du service Docker

  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]  # Utiliser le nom du service Docker
    